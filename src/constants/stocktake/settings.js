const SETTINGS = {
  /**
   * Property used to indicate if the initial sync has been completed
   */
  initialSyncCompleted: 'ROMBO_STOCKTAKES_INITIAL_SYNC',

  /**
   * Indicates the version of the schema database, this is user to to force a re-sync when schema changes
   */
  databaseVersion: 'ROMBO_STOCKTAKES_DB_VERSION'
};

export default SETTINGS;
